# Flox runtime state (regenerated on every activation)
.flox/run/
.flox/cache/
.flox/log/
.flox/lib/

# flox build output symlinks
result
result-*

# Game data is fetched at build time, never committed. Commercial
# DOOM.WAD / DOOM2.WAD must never be committed here or published.
*.wad
*.WAD
*.zip
